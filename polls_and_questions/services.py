from uuid import UUID

# /v1/wbe/watchits/{uuid}/
def check_watchit_uuid(watchit_id: UUID) -> bool:
    """
    Check if watchit_id is valid.

    Args:
        watchit_id: identifier

    Returns: True if watchit_id exist, False in other case.

    """
    # TODO: Implement
    return True
    # return False
